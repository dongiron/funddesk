// RouteOne scraper — stubbed until Slice 3.2 fills in the real scrape +
// payload shape. Establishes the module boundary so the popup can route to it.

export type RouteoneScraperResult = null // filled in Slice 3.2

export async function getRouteoneScrape(): Promise<RouteoneScraperResult> {
  return null
}
